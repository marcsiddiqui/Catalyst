@using Nop.Web.Areas.Admin.Models.HolidaysNEvents
@model HolidayModel

@{
    //page title
    ViewBag.PageTitle = T("Admin.HolidaysNEvents.Holidays.AddNew").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("Holidays");
}

<form asp-controller="Holiday" asp-action="Create" method="post" id="holiday-form">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.HolidaysNEvents.Holidays.AddNew") 
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.HolidaysNEvents.Holidays.BackToList")</a>
            </small>
        </h1>
        <div class="float-right">
            <button type="submit" name="save" class="btn btn-primary">
                <i class="far fa-floppy-disk"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn btn-primary">
                <i class="far fa-floppy-disk"></i>
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>