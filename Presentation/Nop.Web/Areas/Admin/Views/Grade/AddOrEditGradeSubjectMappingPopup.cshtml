@using Nop.Web.Areas.Admin.Models.GradeManagement
@model GradeSubjectMappingModel

@{
    Layout = "_AdminPopupLayout";

    //page title
    ViewBag.PageTitle = T("Admin.GradeManagement.Grades.AddNew").Text;
}

<form>
    <section class="content">
        <div class="container-fluid">
            <div class="content-header clearfix">
                <h1 class="float-left">
                    @T("Admin.Configuration.Grade.Subject.Mapping.Add.New")
                </h1>
                <div class="float-right">
                    <button type="button" name="save" class="btn btn-primary" onclick="saveGradeSubjectMapping()">
                        <i class="far fa-floppy-disk"></i>
                        @T("Admin.Common.Save")
                    </button>
                </div>
            </div>
            @await Html.PartialAsync("_CreateOrUpdateGradeSubjectMapping", Model)
        </div>
    </section>
</form>