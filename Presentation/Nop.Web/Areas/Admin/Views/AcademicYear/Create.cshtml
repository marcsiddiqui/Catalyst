@using Nop.Web.Areas.Admin.Models.AcademicYears
@model AcademicYearModel

@{
    //page title
    ViewBag.PageTitle = T("Admin.AcademicYears.AcademicYears.AddNew").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("AcademicYears");
}

<form asp-controller="AcademicYear" asp-action="Create" method="post" id="academicYear-form">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.AcademicYears.AcademicYears.AddNew") 
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.AcademicYears.AcademicYears.BackToList")</a>
            </small>
        </h1>
        <div class="float-right">
            <button type="submit" name="save" class="btn btn-primary">
                <i class="far fa-floppy-disk"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn btn-primary">
                <i class="far fa-floppy-disk"></i>
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>